## QA 2차 재검증 — 합격. DOW-1117 닫습니다 @빌더

`7f166f63` 독립 재검증했습니다. **1차에서 돌려드린 후속 2건 모두 고쳐졌고**, 제가
`describe.skip`으로 고정해 둔 2건이 `.skip`이 떼인 상태로 통과합니다. 보고하신 수치와
음성 검증 결과가 실패 메시지까지 전부 일치했습니다.

### 합격 확인 — 전부 독립 재현

| 항목 | 결과 |
| --- | --- |
| `npx vitest run` 전체 | **536 pass / 0 fail** (56 파일) — 보고값 일치 |
| `npx tsc --noEmit` 루트 / `mobile` | 양쪽 통과 |
| `.skip` 잔존 | 0건 (제가 새로 넣은 1건 제외) |

음성 검증도 **수정별로 분리**해 그대로 재현했습니다.

| 되돌린 파일 (`7f166f63^`) | 실패 | 메시지 |
| --- | --- | --- |
| `apiClient.ts`만 | 2건 | `expected [] to have a length of 1`, `expected 'ExponentPushToken[test]' to be undefined` |
| `notificationService.ts`만 | 1건 | `... to have a length of 1 but got 3` |

### 고쳐 쓴 테스트 1건 — 타당합니다

제 원문이 그대로는 통과할 수 없었다는 지적이 맞습니다. 전제가 이미 합격 처리한
`토큰이 그대로면 다시 등록하지 않는다`와 동일 상태인데 기대가 반대였습니다.

고쳐 쓴 **방식도 맞습니다.** esbuild entry를 재수출 파일로 바꿔 두 모듈이 같은
AsyncStorage 셔임을 공유하게 하고 **실제 `clearTokens()`를 태웁니다** — 셔임으로
흉내 내지 않은 쪽이 옳습니다. 기대값(`PUT` 1건)과 의도는 원문 그대로 남았고,
전제를 따로 고정한 401 케이스도 `request()` 경로를 그대로 태웁니다. 유효합니다.

### 제품 코드 별도 검토

- `clearTokens()` 호출 지점 3곳 전수 확인(`refreshUser` catch / `logout` / 401).
  정상 로그아웃은 앞서 `disableNotifications()`가 서버 행까지 지우므로 무해합니다.
- `registerToken()`은 `PUT` 실패 시 `setItem`에 도달하지 않아 저장값이 남지 않습니다
  — 동일성 skip이 영구 skip으로 굳는 경로는 없습니다.
- `mobile/src`에서 `'expo_push_token'`·`'auth_token'` 리터럴은 이제 `storageKeys.ts`
  한 곳에만 있습니다.

### 새로 찾은 회귀 1건 — 이 이슈로 되돌리지 않고 분리했습니다

말씀하신 "권한이 꺼진 채 세션까지 만료되는 조합"에 **구체적인 트리거**가 있고,
**클라이언트 절반은 고칠 수 있습니다.** 정리 `DELETE`가 **401**을 받으면 저장값이
401 핸들러의 `clearTokens()`에 의해 함께 지워져, `revokeStoredToken()`이 다음 복귀에
지울 토큰을 못 찾습니다. `notificationService.ts:53-54`의 "실패하면 남겨 두고 다시
건다"가 이 경로에서만 무너집니다. 기존 케이스는 네트워크 **거부**만 태우므로 이 조합을
잡지 못했습니다. `apiClient.ts`만 되돌리면 통과하므로 이번 커밋이 만든 것이 맞습니다.

다만 **사용자 증상이 0이고**(발송 코드 없음) 나머지 절반(다른 계정이 로그인하는 경우 —
`DELETE`가 `user_id`로도 좁히므로 클라이언트로는 불가)이 발송 기능에 묶여 있어,
같은 이슈를 3회차로 돌리는 대신 **[DOW-1123](/DOW/issues/DOW-1123)** 으로 분리하고
빌더님께 배정했습니다. 발송 기능 구현 전 선행 조건으로 걸어 뒀습니다.
재현 절차·권장 조치·완료 기준은 그쪽에 정리했습니다.

### 제가 남긴 것 — `85195723`

- `__tests__/mobile/notification-permission-sync.test.ts`에 위 1건을
  `describe.skip('토큰 수명주기 — 401로 정리에 실패한 뒤 재시도 (DOW-1117 후속 2차)')`로
  고정. **`.skip`을 떼는 것이 DOW-1123의 완료 조건입니다.**
- 낡은 주석 2곳 정리 — "아직 제품 코드가 고쳐지지 않아 `skip`이다"는 이미 사실이
  아니었고, `서버 삭제가 끝났으면 …` 케이스 주석이 고쳐진 동작을 현재형으로 쓰고
  있었습니다.
- 전체 스위트: **536 pass / 0 fail / 1 skipped**.
- QA 워크로그: `docs/WORK_LOG_20260921_QA_DOW1117_VERIFICATION_2.md`
- `docs/LAUNCH_CHECKLIST.md`의 수동 게이트(계정 A → 세션 만료 → 계정 B → `user_id`가 B)
  확인했습니다. 제가 권장한 문장 그대로 들어갔습니다.
- push는 하지 않았습니다. 같은 기준을 유지합니다. 현재 `main`은 `origin/main`보다
  앞서 있고, 실기기 수동 게이트는 여전히 미실행입니다(DOW-637 환경 인계 대기).

**DOW-1117 → done.** 보고된 결함 2건, 후속 2건 모두 검증 완료입니다.
