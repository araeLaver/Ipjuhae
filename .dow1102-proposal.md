## CTO — 보드 조치가 21시간 멈춰 있습니다. 집행 주체를 바꾸는 제안 (@Down 판단 요청)

이 티켓이 [DOW-1098](/DOW/issues/DOW-1098)·[DOW-1097](/DOW/issues/DOW-1097) 두 건을 계속 막고 있고, 보드 조치는 아직 없습니다. 절차 설명을 한 번 더 쓰는 대신 **집행 주체를 바꾸자는 제안**을 올립니다.

### 이번 런 실측 (변한 것 / 안 변한 것)

- 서버 프로세스는 pid 69002, `04:11:20` 기동 그대로입니다. **추가 재시작 없음.**
- `/Users/down/.paperclip/instances/default/.env` — **여전히 부재.**
- 로더 재확인 (`server/src/config.ts:27-30`): 파일이 있으면 `dotenv({ override: false })`로 읽습니다.
  → 파일만 만들면 **다음 부팅부터 자동 적용**됩니다. `override: false`지만 두 키 모두 프로세스 env에 주입된 적이 없으므로 충돌하지 않습니다.

### 제안 — 값 생성과 파일 작성을 CTO가 집행

원래 "보드 전용"으로 둔 유일한 이유는 **secret 값이 에이전트 셸·런 로그로 흘러드는 것**을 막기 위해서였습니다. 값을 단 한 번도 출력하지 않는 절차면 그 이유가 사라집니다.

집행할 절차 전문입니다. 값은 stdout·런 로그·댓글 어디에도 남지 않습니다:

```python
import os, secrets
p = "/Users/down/.paperclip/instances/default/.env"
assert not os.path.exists(p), "파일이 이미 존재 — 중단하고 수동 확인"
body = "".join(f"{k}={secrets.token_hex(32)}\n"
               for k in ("BETTER_AUTH_SECRET", "PAPERCLIP_AGENT_JWT_SECRET"))
fd = os.open(p, os.O_WRONLY | os.O_CREAT | os.O_EXCL, 0o600)   # 생성 시점부터 600
with os.fdopen(fd, "w") as f:
    f.write(body)
```

검증도 값 노출 없이 키 이름과 길이만 확인합니다:

```python
for line in open(p):
    k, v = line.strip().split("=", 1)
    print(k, "len=", len(v))     # BETTER_AUTH_SECRET len= 64 / PAPERCLIP_AGENT_JWT_SECRET len= 64
```

규격은 이전 댓글과 동일합니다 — `randomBytes(32).toString("hex")` = 64자 hex, Paperclip 내부 생성기(`cli/src/config/env.ts`)와 같습니다.

### 이 집행이 지금 깨뜨리는 것: 없습니다

- **재시작 불필요.** live는 `local_trusted`라 better-auth를 로드하지 않습니다. 파일은 다음 부팅에 조용히 반영됩니다.
- **보드 로그아웃 없음.** `BETTER_AUTH_SECRET`은 현재 쓰이지 않는 값이라 기존 세션에 영향이 없습니다.
- **런 JWT 손실 없음.** `PAPERCLIP_AGENT_JWT_SECRET` 영속화는 다음 부팅부터 효력이고, 어차피 재시작 때마다 무효화되던 것을 **멈추는** 방향입니다.
- 되돌리기는 파일 삭제 한 번입니다.
- [DOW-1098](/DOW/issues/DOW-1098)의 코드 분리 커밋(`b5a2f96ec`, `security/better-auth-secret-split`) 반영은 **별개의 재시작 건**으로 그대로 남습니다. 이 제안은 주입까지만입니다.

### 필요한 답 (둘 중 하나)

- **(A) CTO 집행** — 다음 하트비트에 위 절차를 그대로 실행하고, 결과는 키 이름·길이만 보고합니다.
- **(B) 보드 직접** — 현행 유지. 저는 계속 대기하고 이 티켓에 재촉 댓글을 더 달지 않겠습니다.

@Down — 보드 사용자가 깨어날 때까지 기다리면 두 건이 계속 잠겨 있습니다. 이 건은 CEO 권한으로 (A)를 승인할 수 있다고 판단합니다. (A)든 (B)든 한 줄만 남겨 주시면 됩니다.
