## 회귀 테스트 추가 — 첫 시도는 버렸습니다

커밋 `dd89f3db7`. 수정 커밋 `171a066a5`와 함께 `pending-restart`에 있습니다.

### 첫 시도는 쓸모가 없어서 버렸습니다

처음엔 **쓰기 타이밍**으로 잡으려 했습니다 — "덤프가 도는 도중에 파일이 조금씩 커지는가". 테이블을 통째로 올리는 옛 코드는 동기 루프가 이벤트 루프를 붙들고 있으니 중간 관측이 안 될 거라고 봤습니다.

**틀렸습니다.** 옛 코드로 돌려 보니 그 테스트도 통과합니다. 행 루프가 끝난 뒤 이어지는 `await`들에서 `pendingWrite` 체인이 단계적으로 빠져나가, 샘플러가 결국 "조금씩 커지는" 모습을 그대로 봅니다. 신호가 두 구현을 구분하지 못합니다.

증명하지 못하는 테스트를 남기는 것보다 버리는 게 낫다고 판단해 지웠습니다.

### 두 번째 — 힙을 직접 재는 방식

구분되는 건 타이밍이 아니라 **힙**이므로, 힙을 재게 했습니다.

- 약 **240MB** 테이블을 만들고
- 덤프를 **`--max-old-space-size=128`로 묶은 자식 프로세스**에서 실행합니다
- 테이블을 배열로 올리면 들어갈 수 없고, 500행씩 흘리면 들어갑니다

### 음성 검증 (핵심)

`171a066a5^`(수정 전)에 대고 같은 테스트를 돌렸습니다:

```
× dumps a table far larger than the heap without running out of memory
  → expected '(node:97321) …' not to match /JavaScript heap out of memory/
```

**서버를 시간마다 죽이고 있는 바로 그 V8 abort가 테스트 안에서 그대로 재현됩니다.** 수정본에서는 통과합니다. 파일 전체 3 passed.

### 정정 — 방금 생긴 크래시 리포트는 서버가 아니라 제 테스트입니다

`node-2026-09-21-092026.ips`가 새로 생겼는데, 이건 **위 음성 검증이 일부러 OOM시킨 자식 프로세스**입니다 (pid 97321, 수명 2초, 09:20:20 기동 → 09:20:22 사망). 서버 크래시로 오독하지 않도록 적어 둡니다.

서버 본체(pid 46123, 08:19:59 기동)는 이 글을 쓰는 09:21 시점에 **61분째 살아 있고 아직 안 죽었습니다.** 지금까지 주기가 62~64분이었으니 곧 죽을 차례입니다.

### 다음 — 배포와 검증

재시작 요청은 따로 하지 않습니다. 다음 크래시가 `pending-restart`의 두 커밋을 그대로 싣고 올라옵니다.

그 크래시가 제 run JWT도 같이 무효화하므로 이 heartbeat는 거기서 끊깁니다. 다음 웨이크에서 이어받아 검증합니다:

1. 새 프로세스가 `171a066a5` 이후 코드로 떴는지
2. **백업 주기 3회(약 3시간)** 를 크래시 없이 넘기는지 — 이게 합격 기준입니다
3. `Automatic database backup complete` 로그가 정상으로 찍히는지

### 아직 안 고친 곁가지 2건

본문에 적은 대로입니다. 이 이슈에서 같이 처리하지 않고 별도로 분리하겠습니다.

- **보존(retention) 미동작** — 505개 / 731GB, 최고령 48일 (`retentionDays=30`)
- **launchd 로그 0바이트** — 크래시 포렌식 공백
