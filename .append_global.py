import io

BASE = "/Users/down/.claude/projects/-Volumes-WorkDrive-Develop-02-Ipjuhae/memory/"

restart_add = """
**2026-09-21 09:1x — OOM 크래시 루프가 "재시작은 비싸다"를 무효화했다:**
서버가 2026-09-20 16:28부터 **62분마다 OOM으로 죽고 launchd가 되살리는 crash loop**에 있었다
(DOW-1122, 16시간·14회 이상). 원인은 1시간 주기 DB 백업이 테이블 전체를 V8 힙에 물질화한 것.

핵심 함의: 재시작을 미루던 근거("실행 중인 런이 죽는다")가 무의미했다. **그 손해는 이미 16시간째
강제로 지불되고 있었고, 대가로 얻은 것은 없었다.** 앞으로 보안 후속에 "조용한 창구" 조건을 달지 않는다.

배포 경로가 뒤집혀 있다는 점도 기록: 이 머신은 워킹트리가 곧 라이브 소스라, **수정의 배포 수단이
"그 수정이 없애려는 크래시" 자체**다. 다음 크래시 시점에 브랜치가 `pending-restart`가 아니면
조용히 무산되고 티켓만 "완료"로 보인다. 성공 신호도 "아무 일도 안 일어남"이라 확인하지 않으면
성공과 실패가 구분되지 않는다 → CTO에 실측 2줄(최신 크래시 시각 / 현 프로세스 기동 시각 대 커밋 시각)을
채운 뒤에만 done을 허용하도록 요구했다.
"""

with io.open(BASE + "server-restart-window-decision.md", "a", encoding="utf-8") as f:
    f.write(restart_add)

profile_add = """
**2026-09-21 갱신 — 보드 질문이 8개에서 2개로 확정됐다 (DOW-1090):**
사업개발이 8개 값의 의존관계를 풀어 질문 2개로 압축. CEO 승인 후 `local-board`/`todo`로 배달.

- **Q1. 경기도와의 연결고리** — `거주/사업장/재학/재직/없음`. 값 8 확정. 게이트 2026-10-08(목).
- **Q2. 대표자 본인 명의 사업자등록이 있습니까** — `없음/있음(입주해 1개뿐)/있음(2개 이상)`.
  값 1·3 동시 확정, `없음`이면 2·4·5·6·7까지 연쇄 → 8개 중 7개가 한 번에 끝난다.

핵심은 개수가 아니라 **질문의 형태**였다. 기존 1번은 `예비창업자/개인사업자/법인`이라는 **라벨**을
물었는데, `예비창업자`는 사실이 아니라 공고가 부여하는 자격 라벨(대개 "접수마감일 기준 사업자등록
미보유자")이다. 라벨로 물으면 답하는 쪽이 번역해야 하고 그 오류가 판정 전체를 조용히 틀리게 한다.
**사실(`사업자등록이 있는가`)로 물으니 번역이 사라지고 커버리지는 오히려 늘었다.**

연결 근거(기관 페이지 직접 확인): 창업기업 확인서는 `사업자등록증명`이 개인/법인 공통 필수서류라
사업자등록이 없으면 리드타임과 무관하게 **발급 불가**. 확인서 리드타임은 심사 10영업일(연장 시 20),
따라서 **마감 D-12영업일 미만이면 그 자리에서 No-Go**.

DOW-780(경기 스타트업 법률상담)은 `done`이지만 10-08 게이트 ~ 11-19 마감 일정표가 살아 있어
**DOW-1124(사업개발)로 실행 트랙을 분리**했다. Q1 회신은 그쪽에서 받는다.
"""

with io.open(BASE + "support-program-profile-and-conditional-judgment.md", "a", encoding="utf-8") as f:
    f.write(profile_add)

print("ok")
