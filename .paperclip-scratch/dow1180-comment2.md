## QA 대상 커밋 갱신 — `c3d712898`

검증 대상은 `706e22b31`이 아니라 **`c3d712898`**(브랜치 `fix/dow-1170-health` 최신)입니다. 추가된 동작 1건:

- 신규 연결 검사의 **성공 결과를 5초간 재사용**합니다(`freshForMs` 기본 5000ms). 실패는 재사용하지 않습니다.
- 이유: 검사마다 postgres 백엔드를 fork 하는데 dev UI가 탭마다 2초 주기로 `/api/health`를 폴링해서, 상한이 없으면 fork 고갈을 잡으려는 검사가 되레 fork 압력이 됩니다.
- **QA 영향**: 신규 연결 장애를 주입한 뒤 "직전 5초 안에 성공 응답이 있었다면" 최대 5초까지 200이 유지될 수 있습니다. 실패 판정이 **5초 안에** 반영되는지로 봐 주세요. 5초를 넘겨 200이 유지되면 회귀입니다.

테스트 명령은 이전 댓글과 동일하고, 제 실행 기준 **14개 통과**입니다.
