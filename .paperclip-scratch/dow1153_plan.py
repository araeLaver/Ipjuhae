import os, json, urllib.request

base = os.environ['PAPERCLIP_API_URL']
k = os.environ['PAPERCLIP_API_KEY']
run = os.environ['PAPERCLIP_RUN_ID']
ISSUE = 'ae713ff2-f225-421f-bfef-41559ff63afa'

body = """# DOW-1153 실행계획 — ETRI 위치인증(PoL) 공모

- 마감: **2026-10-16(금) 18:00** (2026-09-24 기준 D-22)
- 현재 단계: 1차 산출 완료, **보드 결정 대기**
- 전체 초안 본문: 저장소 `docs/business-development/20260924_etri_pol_business_model_proposal_draft.md` (커밋 `d79984c0`, origin/main 반영 완료)

## 1. 완료 — 사업모델 기획서 초안

공고 제출서류 ①번에 해당한다. 핵심 논지는 다음 한 줄이다.

> 허위매물·허위후기·보증금 분쟁은 형태가 달라도 실패 지점이 하나다. **주장하는 사람과 물리적 장소 사이의 연결이 끊겨 있다.**

**결합 구조** — 입주해 신뢰 엔진(migration 024, 테이블 23종)은 이미 다음 계층으로 구현되어 있다.

```
trust_source_registry → trust_evidence_nodes → trust_fact_nodes
  → trust_derived_nodes / trust_score_runs
  → trust_dependency_edges + trust_change_events (변경 전파)
```

ETRI PoL은 **`trust_source_registry`의 새 출처 한 건**으로 들어온다. `source_type='partner_api'`는 기존 CHECK 제약이 이미 허용하는 값이고, 각 위치증명은 `trust_evidence_nodes` 한 행(`object_hash`, `observed_at`, `valid_until`, `consent_id`)이 된다. **신규 테이블 생성도, 기존 스키마 변경도 필요 없다.**

그래서 PoC가 검증해야 할 질문은 "연동이 되는가"가 아니라 **"측위 신뢰도를 신뢰점수에 어떤 가중치로 환산해야 시장에서 의미가 있는가"** 다.

**유스케이스 3종**

| # | 내용 | 실패 시 동작 |
|---|---|---|
| UC-1 | 현장확인 매물 배지 — 등록자가 그 주소지에서 발급한 위치증명 첨부 시에만 배지 + 상위 노출 | 배지 미부여. 등록 자체는 막지 않는다 (차단이 아니라 차등) |
| UC-2 | 거주 검증 후기 — 그 주소지 위치증명이 있을 때만 신뢰점수에 반영 | 게시는 되되 가중치 0 |
| UC-3 | 입주 체크리스트 증거력 — 상태 기록에 위치·시각 증명 결합 | PoC 범위 외 |

**PoC 측정지표 4종** — 기술 2개(측위 성공률, 오인정률)와 사업성 2개(등록자 이탈률, 배지 효과 전환율 차이)를 같이 잰다. 둘을 함께 재야 기술이전 본계약의 근거가 된다는 것이 이 설계의 입장이다.

## 2. 완료 — 도메인 역량 증빙 목록

공고 제출서류 ③번에 해당한다. 전부 이미 존재하는 산출물이다.

- 특허 출원 `10-2026-0126389` + 구현증빙 4종 (`docs/patent/` 매트릭스·API증빙·해시 매니페스트, `docs/TRUST_PLATFORM_OPERATIONS.md`)
- 제품 규모 실측 (2026-09-24): 커밋 438 / 마이그레이션 48 / API 라우트 136 / 페이지 80 / 테스트 파일 61 / 신뢰 엔진 테이블 23 / 개발기간 8개월
- **위치·규제 사전검토 이력 2종** — 2026-08-24 KISA 적용성 검토, 2026-09-02 위치정보 보호조치 체크리스트. 심사에서 "기술을 받아서 무엇을 할지 모르는 팀"과 구분되는 지점이다
- 사업개발 실행 이력 47건 (`docs/business-development/`)

## 3. 이번 검토에서 새로 드러난 사항 — 위치정보 규제

이슈 본문에 없던 내용이고 의사결정에 영향을 준다.

코드베이스 전수 검색 결과 `navigator.geolocation` / `getCurrentPosition` / `watchPosition` / `expo-location` 사용처가 **0건**이다(2026-09-24 실측). 2026-08-24 KISA 검토의 전제 — "입주해는 단말 위치를 수집하지 않는다" — 는 오늘도 유효하다.

**PoL 도입은 그 전제를 깬다.** 도입 즉시 개인위치정보 처리가 시작되므로 아래가 신규 과제로 발생한다.

| 쟁점 | 상태 |
|---|---|
| 위치기반서비스사업 신고 의무 / 소상공인·1인창조기업 면제 해당 여부 | **미확인 — 법률 확인 필요** |
| 개인위치정보 동의 항목 신설 (기존 `data_consents` 재사용 가능) | 설계 필요 |
| 좌표 원본 미보관·증명 해시만 보관 설계의 적법성 | 미확인 |

**기획서 제출은 막지 않는다.** 공고가 신고 이력을 요구하지 않고 실제 위치정보 처리는 협약·PoC 단계에서 시작되기 때문이다. 다만 **협약 전까지는 반드시 해소되어야 한다.**

**연계 제안**: [DOW-1124](/DOW/issues/DOW-1124) 경기 스타트업 지원센터 법률상담 트랙(10-08 게이트 ~ 11-19 마감)에 "위치기반서비스사업 신고 의무 및 면제 요건"을 의제로 추가한다. 일정이 맞는다 — 법률상담이 공모 발표평가·협약보다 앞선다.

## 4. 제출서류 상태

| # | 서류 | 상태 |
|---|---|---|
| ① | 사업모델 기획서 | **초안 완료**. 공고 지정 서식 확인 후 이식 필요 |
| ② | 사업자등록증 → 예비창업자는 경력증명서 등으로 대체 | **미확보 — 보드만 확보 가능** |
| ③ | 도메인 역량 증빙 | **목록 확정**. 패키징 형태 확인 필요 |
| ④ | 재무제표 (해당 시) | 해당 없음으로 판단 |
| ⑤ | 기타 (실증 협조 공문 등) | 협약 단계 서류로 판단. 운영사 확인 시 함께 질의 |

## 5. 남은 일정

| 날짜 | 작업 |
|---|---|
| ~09-30 | 공고 지정 서식 확보 및 본문 이식 |
| ~10-02 | 운영사 (주)티투비 문의 4건 실행 — **승인 후** |
| ~10-07 | 증빙 ③ 패키징 |
| ~10-08 | 경기 법률상담 게이트에 위치정보법 의제 반영 |
| ~10-10 | ② 대체 증빙 확보 — **보드 실행** |
| ~10-14 | CEO 검토·승인 |
| **10-16 18:00** | **절대 마감** |

## 6. 보드 결정 대기 3건

1. **이 공모에 실제로 응모하는가.** 응모 시 3장의 위치정보 규제 과제를 협약 전까지 해소하는 것을 함께 수용하는 것이다.
2. **운영사 문의 4건을 실행해도 되는가.** 외부 연락이므로 승인 대상으로 본다. 질의는 ① 대체 증빙 인정 범위 ② 발표평가 대면 여부·시기 ③ 기획서 지정 서식·분량 ④ PoC 단계 개인위치정보 처리의 법적 지위(ETRI가 위치정보사업자로 제공하는지, 신청기업이 직접 처리하는지).
3. **② 대체 증빙으로 무엇을 제출할 것인가.** 대표자 개인 경력 서류이므로 보드만 확보할 수 있다.

**1번이 No면 2·3번은 소멸한다. 1번이 이 트랙의 병목이다.**

## 7. 경계 — 하지 않은 것

- 접수·제출하지 않았다
- 운영사에 문의하지 않았다
- 경력증명서 등 대체 증빙을 발급받지 않았다
- 위치정보법 적용 여부에 대한 법률 판단을 내리지 않았다 (쟁점만 세웠다)
"""

payload = json.dumps({
    'title': 'Plan',
    'format': 'markdown',
    'body': body,
    'baseRevisionId': None,
}).encode()

req = urllib.request.Request(
    base + '/api/issues/' + ISSUE + '/documents/plan',
    data=payload,
    method='PUT',
    headers={
        'Authorization': 'Bearer ' + k,
        'Content-Type': 'application/json',
        'X-Paperclip-Run-Id': run,
    },
)
print(urllib.request.urlopen(req).read().decode()[:300])
