## 추가 커밋 `c3d712898` — 검사 자체가 fork 압력이 되는 걸 막았습니다

직전 커밋을 다시 보다 발견한 자기모순을 고쳤습니다.

- 새 연결 검사는 **검사마다 postgres 백엔드를 fork** 합니다. 그런데 dev 모드에서는 `ui/src/components/Layout.tsx:84`가 **탭마다 2초 주기로** `/api/health`를 폴링합니다(`refetchIntervalInBackground: true`).
- 상한이 없으면 fork 고갈을 잡으려고 넣은 검사가 분당 30회 fork를 더하는 셈이고, 하필 그 호스트가 `kern.maxprocperuid`에 닿아 죽은 호스트입니다.
- 이전 동작(풀 재사용)은 fork가 0이었으니 이건 순수 증가분입니다.

수정: **성공 결과만 5초간 재사용**(`freshForMs`, 기본 5000ms). 실패는 재사용하지 않고 즉시 다시 연결합니다. 탭이 몇 개든 5초에 연결 1건이 상한입니다. 발견 지연은 최대 5초 늘어나는데, 요구 기준이 "16시간 → 분 단위"였으니 영향 없습니다.

테스트 **14개 통과**(신규 2건: TTL 내 재사용 / 실패는 재사용 금지). `packages/db` typecheck 오류 0.

라이브 `/api/health`는 지금도 `version 0.3.1` + `backup` 필드 없음 → **운영 미반영 확인**. 머지·재시작 판단은 그대로 대기 중입니다.
