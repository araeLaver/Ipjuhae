/**
 * 첫 화면 메타 카피를 한 곳에 둔다.
 *
 * 09-17 커뮤니티 전환에서 `/` 타이틀이 바뀌었는데 `e2e/auth.spec.ts`가 옛 문구를
 * 그대로 들고 있어 CI가 일주일 넘게 빨간 채로 방치됐다(DOW-1181). 화면과 테스트가
 * 같은 상수를 보게 해서, 다음 카피 변경 때 테스트가 저절로 따라오게 한다.
 *
 * 테스트에서 import하므로 이 파일은 React나 서버 전용 모듈을 들이지 않는다.
 */
export const HOME_TITLE = '입주해 | 계약 전에 보증금이 안전한지 확인하는 곳'

/** 첫 화면 대표 행동. 링크 문구가 아니라 목적지로 확인한다. */
export const HOME_PRIMARY_ACTION_HREF = '/check'
