import io

PATH = ("/Users/down/.paperclip/instances/default/companies/"
        "0662097f-7363-4fc0-ac51-45798f6dddf0/agents/"
        "f6b770fa-e6aa-49b4-8e02-82a63a57e3b8/memory/2026-09-21.md")

ADD = """

## 09:1x heartbeat — DOW-1090 보드 배달, DOW-1124 신규, DOW-1122 검증 요구

### DOW-1090 — 승인하고 보드로 배달

사업개발이 8개 값을 질문 2개로 압축해 `in_review`로 올렸다. 승인하고 `local-board`/`todo`로 라우팅했다(R1). 회신 경로와 보드 앞 4건의 우선순위표를 같이 냈다.

채택 근거는 **개수가 아니라 질문의 종류**다. 나는 8 → 1 → 2로 *개수*만 줄였다. 사업개발은 형태를 바꿨다.

> `예비창업자 / 개인사업자 / 법인 중 고르세요`(라벨) → `사업자등록이 있습니까`(사실)

라벨로 물으면 답하는 사람이 자기 상태를 공고 용어로 번역해야 하고, 그 번역이 틀리면 판정 전체가 조용히 틀린다. 사실로 물으면 번역이 없고, 같은 답이 값 1·3을 동시에 채우고 `없음`이면 2·4·5·6·7까지 끌고 간다.

**일반화: 회신을 만드는 건 개수 축소가 아니라 형태 전환이다.** 어제 나는 "답할 곳이 몇 군데인가 → 그 자리가 보이는가"로 한 겹 내려갔는데, 여기서 한 겹 더 있다는 걸 배웠다. 보이는 자리에 도달한 뒤에는 **답하는 데 드는 인지 비용**이 다음 병목이다.

### DOW-1124 — 닫힌 이슈가 살아 있는 마감을 들고 있었다

DOW-780은 상태가 `done`인데, 그 안에 2026-10-08 게이트부터 11-19 마감까지의 역산 일정표가 그대로 살아 있었다. 닫힌 이슈는 아무도 깨우지 않는다. Q1에 답이 와도 10-29 접수를 실행할 창구가 없었다.

8~9월 판정 3건 소멸과 같은 모양인데 원인이 반대다. 그때는 **값이 없어서**였고, 이번엔 **값이 와도 받을 데가 없어서**다.

실행 트랙을 별도 이슈로 열어 사업개발에 배정했다.

**일반화: 미래 날짜가 박힌 산출물을 만들었으면, 그 날짜를 소유할 열린 이슈가 따로 있어야 한다.** 산출물 이슈를 닫는 것과 실행 트랙을 여는 것은 별개 행동이다.

### DOW-1122 — 수정은 됐는데 배포는 아직이다

서버가 62분마다 OOM으로 죽는 크래시 루프. CTO가 원인(시간별 DB 백업이 테이블을 통째로 힙에 올림)을 찾고 커서 스트리밍 + `drain()`으로 고쳐 `171a066a5`로 커밋했다. 특히 **커서만으로는 부족했다**는 두 번째 발견이 좋다 — `emit()`이 동기라 덤프 루프가 이벤트 루프에 제어를 안 넘기면 잘라도 체인에 쌓이기만 한다. 첫 수정만 하고 보고했으면 그대로 계속 죽었다.

내가 실측한 것: 마지막 OOM은 `node-2026-09-21-082002.ips`(08:20:02)이고 현 프로세스는 그때 기동했다. 즉 **수정이 안 들어간 버전이 돌고 있다.** 워킹트리는 `pending-restart` HEAD에 물려 있어 다음 크래시가 배포한다.

위험 두 개를 댓글로 고정했다.

1. **배포 경로가 "고치려는 그 크래시" 자체다.** 이 머신은 워킹트리가 곧 라이브 소스라, 다음 크래시 시점에 브랜치가 `pending-restart`가 아니면 수정이 조용히 무산된다. 티켓은 "수정 완료"로 보이는데 루프는 계속된다.
2. **성공 신호가 "아무 일도 안 일어남"이다.** 확인하지 않으면 성공과 실패가 구분되지 않는다.

실측 2줄(최신 크래시가 08:20 이후이면서 그 뒤 62분 이상 새 항목 없음 / 현 프로세스 기동이 커밋보다 뒤)을 채운 뒤에만 `done`을 허용했다. DOW-1110의 "완료로 보고됐지만 확인 안 된 작업" 패턴을 반복하지 않기 위해서다.

### 재시작 비용을 재평가했다

재시작을 미루는 근거는 늘 "실행 중인 런이 죽는다"였다. 그런데 이 크래시 루프가 **2026-09-20 16:28부터 16시간 동안 14회 이상 그 손해를 이미 강제로 물리고 있었다.** 아끼려던 비용은 애초에 지불되고 있었고, 대신 아무것도 얻지 못했다.

보안 후속(DOW-1102 → DOW-1098 → DOW-1097)에 "조용한 창구"를 조건으로 달지 않는다. 집행 조건 4개만 본다. 순서는 유지한다.

### 그 밖

- DOW-1084는 blocked-dedup 적용해 건드리지 않음(내 마지막 댓글 00:13, 이후 외부 댓글 없음).
- 회사 상태: open `todo`/`in_progress` 14건 전부 담당자 있고 갱신 0~1일. 정체 항목 없음. 어제 방치돼 있던 DOW-909는 사업개발 배정 완료.
- 어제 보드로 돌렸던 DOW-447·DOW-637은 그 뒤 각각 사업개발·CTO 담당으로 재배정돼 있다(보드 대기가 아니라 실행 대기). 보드 인박스에 남은 건 DOW-841·DOW-912·DOW-366·DOW-1069 + 오늘 올린 DOW-1090.
"""

with io.open(PATH, "a", encoding="utf-8") as f:
    f.write(ADD)
print("appended", len(ADD))
