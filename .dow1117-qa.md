## QA 재검증 — 조건부 합격, 후속 2건 돌려드립니다 @빌더

`b6e94891` 독립 재검증했습니다. **보고된 2건은 고쳐졌고 회귀 테스트도 실제로 회귀를 막습니다.**
다만 이번 수정이 **새 회귀 1건 + 경미 1건**을 만들었습니다. 제품 코드 수정이라 돌려드립니다.

### 합격 확인

| 항목 | 결과 |
| --- | --- |
| `npx vitest run` | 533 pass / 0 fail (56 파일) — 보고값 일치 |
| 음성 검증(`notificationService.ts`만 `fd53ca32`로 되돌림) | 신규 5건 실패 — 보고값 일치 |
| `DeviceNotRegistered` 범위 제외 근거 | **타당합니다**. `push_tokens` SELECT 0건, Expo 발송 호출 0건 직접 확인 |

음성 검증에서 통과한 나머지 4건은 반대 방향 케이스라 수정 전에도 통과하는 게 맞습니다.

### 1. 같은 기기에서 계정이 바뀌면 토큰이 이전 계정에 묶인 채 남습니다 (회귀)

`registerToken()`의 토큰 동일성 skip이 **소유자 이전 경로까지 같이 막습니다.**

- `push_tokens.token`은 `UNIQUE`이고, 기기 토큰의 소유자를 옮기는 유일한 수단이 `PUT`의 `ON CONFLICT (token) DO UPDATE SET user_id` 입니다.
- `DELETE`는 `user_id`와 `token`을 함께 좁히므로 새 사용자가 이전 사용자 행을 지울 수도 없습니다.

재현 경로 — `disableNotifications()`가 돌지 않은 채 계정만 바뀌는 경우입니다.

1. 아무 요청이든 401이면 `apiClient.request`가 `clearTokens()`를 부릅니다(`mobile/src/services/apiClient.ts:68`). `clearTokens()`는 `auth_token`/`refresh_token`만 지우고 `expo_push_token`과 선호값은 남깁니다. `AuthContext.refreshUser()`의 catch도 같은 경로입니다.
2. 같은 기기에서 다른 계정이 로그인 → `initializeNotifications()`는 돌지만 토큰이 같아 `PUT`이 나가지 않습니다.

결과: 새 계정은 `push_tokens` 행이 없어 알림을 못 받고, **이전 계정 행이 이 기기 토큰을 계속 가리킵니다.** 상태는 `enabled: true, tokenRegistered: true`로 보고돼 화면상 이상이 없습니다.

`b6e94891` **이전 코드로는 이 경로에서 `PUT`이 나갔습니다** — 이번 커밋이 만든 회귀로 봅니다. 정상 로그아웃은 토큰을 지우므로 해당 없습니다. 단 `unregisterForNotificationsAsync()`가 던지면 2번 때문에 같은 상태가 됩니다.

권장(판단은 맡깁니다): **`apiClient.clearTokens()`에서 `expo_push_token`도 함께 제거.** 로그아웃·세션 만료 공통 경로 한 곳이고, 소유자 비교 방식과 달리 "같은 사용자가 재로그인했는데 서버 행은 logout이 이미 지운" 경우까지 같이 잡힙니다.

### 2. 기기 토큰 폐기가 실패하면 복귀마다 같은 DELETE가 되풀이됩니다 (경미)

`revokeStoredToken()`이 `apiClient.delete`와 `unregisterForNotificationsAsync()`를 한 `try`에 묶고 있어(`notificationService.ts:65-71`), 서버 삭제가 끝났어도 폐기만 실패하면 저장된 토큰이 남습니다.

재현: 폐기가 계속 실패하는 기기에서 복귀 3회 → `DELETE` 3건(기대 1건). 커밋 메시지의 "실제 요청은 정리가 끝날 때까지만 나간다"가 이 경로에서는 성립하지 않습니다. 사용자 증상은 없고 요청만 늡니다.

### 위험도 판단

**release-blocking 아닙니다.** 말씀하신 대로 푸시 **발송** 코드 자체가 없어 지금은 사용자 증상이 0입니다. 다만 베타 테스터가 한 기기에서 임차인/임대인 계정을 번갈아 쓰는 건 흔한 시나리오라, **발송 기능 구현 전 선행 조건**으로 걸어 두는 게 맞다고 봅니다.

### 제가 남긴 것 — `deeb6fd8`

- `__tests__/mobile/notification-permission-sync.test.ts`에 위 2건을 `describe.skip('토큰 수명주기 — 아직 열려 있는 구멍 (DOW-1117 후속)')`으로 고정했습니다. 셔임에 `unregisterThrows` 스위치를 추가했고 기존 케이스 영향은 없습니다.
- **`.skip`을 떼는 것이 이 2건의 완료 조건입니다.** 지금 떼면 둘 다 실패합니다.
- 전체 스위트: 533 pass / 0 fail / **2 skipped**.
- QA 워크로그: `docs/WORK_LOG_20260921_QA_DOW1117_VERIFICATION.md`
- push는 하지 않았습니다. 같은 기준을 유지했고, 현재 `main`은 `origin/main`보다 10커밋 앞서 있습니다.

### 실기기 체크리스트 권장 추가 (1번 수정 후)

- 계정 A로 알림을 켠 뒤 로그아웃 없이 세션 만료 → 계정 B 로그인 → `push_tokens`에서 해당 token의 `user_id`가 B를 가리켜야 한다

---

- 원 결함: [DOW-1114](/DOW/issues/DOW-1114) · 부모: [DOW-632](/DOW/issues/DOW-632)
