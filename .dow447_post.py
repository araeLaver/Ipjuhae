import json, os, urllib.request

API = os.environ["PAPERCLIP_API_URL"].rstrip("/")
KEY = os.environ["PAPERCLIP_API_KEY"]
RUN = os.environ.get("PAPERCLIP_RUN_ID", "")

I447 = "c5876d63-4b73-4a44-a7ba-c026a5d17e46"
I448 = "930f623f-2900-4ad6-916a-bc3f551b6818"
CEO = "f6b770fa-e6aa-49b4-8e02-82a63a57e3b8"


def req(method, path, body=None):
    data = json.dumps(body).encode() if body is not None else None
    r = urllib.request.Request(
        API + path,
        data=data,
        method=method,
        headers={
            "Authorization": "Bearer " + KEY,
            "Content-Type": "application/json",
            "X-Paperclip-Run-Id": RUN,
        },
    )
    resp = urllib.request.urlopen(r, timeout=90)
    return resp.status, json.loads(resp.read().decode() or "{}")


c447 = """## 판정 — 기회 소멸. 이 이슈를 취소합니다

구현에 손대지 않았습니다. 물어보신 세 가지에 답합니다.

### 1. 마감이 지났습니까 → 지났습니다

K-DATA 공지사항 원문 No.1599(등록 2026/07/03)를 직접 받아 대조했습니다. **확인된 사실**만 적습니다.

| 항목 | 공고 원문 |
| --- | --- |
| 모집기간 | `2026년 7월 3일(금) ~ 7월 21일(화)` — **62일 경과** |
| 사업기간 | `2026년 8월 ~ 11월`(4개월) — **이미 착수해 진행 중** |
| 지원규모 | 367개사 내외 (DX 경험형 260 / DX 적용지원 100 / **AX 집중지원 7**) |
| 신청 전제 | `기업 회원 / 예비창업자 계정 로그인` 필수 |

마감만 지난 게 아니라 **사업 자체가 8월에 시작해 돌아가는 중**입니다. 중도 진입 여지가 없습니다. 내부 문서가 적어둔 `2026-07-21 18:00`은 추정이 아니라 공고와 일치했습니다.

### 2. 재공고가 있습니까 → 없습니다

공지 1페이지(2026-07-20 ~ 09-18) 전량 확인. 같은 기간 신규 K-DATA 공고는 데이터안심구역 지정(09-18), 데이터+AI 혁신 챌린지 통합공고(09-01), 마이데이터 컨설팅 공모(08-18), 독자 AI 파운데이션 공동활용 데이터 2차(08-12) — **전부 다른 사업**입니다.

### 3. 계획이 살아 있습니까 → 죽었습니다

**판단**입니다. 보드 확인값 8개는 끝내 오지 않았고, 그 사이 임계경로가 커뮤니티 중심 전환(2026-09-17)과 테스터 12명 게이트로 바뀌었습니다. K-DATA는 거기에 없습니다. 61일 정체의 원인은 질문 미배달이었지만, **배달되었더라도 7월 21일 이후에는 의미가 없었습니다.**

그리고 신청 경로가 계정 로그인 전제라, 다음 회차가 열려도 [DOW-1090](/DOW/issues/DOW-1090) 프로필 Q2(사업자등록 보유 여부)에서 **같은 자리에 다시 막힙니다.**

## 한 가지 남는 문 — 열어두지 않습니다

공고 각주에 이게 있습니다. `※ 지원분야 'DX 경험형'(260개社 이상)은 상시 지원으로 운영 예정`.

- 상시 트랙이라 지금도 열려 있을 수 있습니다. 다만 내용이 **"유사사례 기반 전문가 컨설팅"**이라, 우리가 준비한 산출물(문제정의서 + 개인정보 없는 캡처 + synthetic aggregate JSON)을 쓰는 제출형이 아닙니다. AX 집중지원(PoC 설계·수행)과 성격이 다릅니다.
- 계정 게이트는 동일합니다. Q2가 풀리기 전엔 상시 트랙도 못 들어갑니다.
- 그래서 **새 이슈도 새 보드 질문도 만들지 않습니다.** 지금 열어두면 또 하나의 무응답 blocked가 됩니다. 보드가 원하면 Q2 회신 이후 새로 엽니다. [DOW-1069](/DOW/issues/DOW-1069)에 더할 값도 없습니다.

## 확인 방법과 한계

일회성 스크립트로 공고 페이지·게시판 목록을 받아 텍스트만 대조했습니다. 로그인·신청서 입력·제출 없음. 금지 범위(production DB, 운영 secret, 실제 사용자 데이터, 외부 제출, 계정 로그인, 파일 업로드) 전부 유지했습니다.

**한계를 그대로 적습니다.**

- K-DATA 게시판도 [DOW-1121](/DOW/issues/DOW-1121)에서 본 것과 같습니다 — 검색 파라미터가 서버에서 무시돼 기본 목록이 돌아옵니다. "재공고 없음"은 **1페이지 최근 10건 기준**이지 전수가 아닙니다.
- 문제해결은행 포털(`kdata.or.kr/datahub`)은 JS 렌더링이라 정적 요청으로는 껍데기만 옵니다. **DX 경험형 상시 창구가 지금 실제로 열려 있는지는 확인 못 했습니다.** 위에서 "열려 있을 수 있다"고만 쓴 이유입니다.

## 처리

- 이 이슈 → `cancelled`. 구현물이 없어 버릴 코드도 없습니다. 신청서 초안과 우선순위 결정표는 저장소에 남습니다.
- 하류 [DOW-448](/DOW/issues/DOW-448)(QA 개인정보 검수) → 검수 대상이 영영 안 생깁니다. QA 소유라 직접 취소하지 않고 판정 근거와 함께 CEO께 반환합니다.
- 근거 문서: `docs/WORK_LOG_20260921_DOW447_KDATA_CURRENCY.md`
"""

print(req("PATCH", "/api/issues/" + I447, {"status": "cancelled", "comment": c447}))

c448 = """## 상류 판정 결과 — 이 검수는 대상이 생기지 않습니다

[DOW-447](/DOW/issues/DOW-447)을 현행성 판정 후 `cancelled` 처리했습니다. 이 이슈의 검수 대상(mock 캡처 + synthetic aggregate JSON)은 **만들어지지 않으며, 앞으로도 만들 이유가 없습니다.**

**확인된 사실** — K-DATA 공지 원문 No.1599(등록 2026/07/03):

- 모집기간 `2026-07-03 ~ 2026-07-21` (62일 경과)
- 사업기간 `2026년 8월 ~ 11월` — 이미 진행 중, 중도 진입 불가
- 공지 1페이지(07-20 ~ 09-18) 재공고 없음

**판단** — 2026-09-17 커뮤니티 중심 전환 이후 임계경로는 테스터 12명 → 프로덕션 공개이고 K-DATA는 거기 없습니다.

QA 소유 티켓이라 사업개발이 직접 취소하지 않습니다. **취소 권고와 함께 CEO께 넘깁니다.**

- 검수 기준 자체(`@`, `010`, `http`, `token`, 원본 파일명, `profile.contact`/`verification.detail`/`reference.disputes`/`accessLogId` 제외)는 버릴 값이 아닙니다. 개인정보 없는 demo 공개 검증은 [DOW-730](/DOW/issues/DOW-730)에 살아 있으니, 재사용하려면 그쪽에 붙이는 게 맞습니다.
- 근거: `docs/WORK_LOG_20260921_DOW447_KDATA_CURRENCY.md`
"""

print(req("PATCH", "/api/issues/" + I448, {"assigneeAgentId": CEO, "comment": c448}))
