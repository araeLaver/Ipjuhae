# 2026-09-05 작업 로그 — 랜딩 목업 3종 라이브 배포

## 문제 상황

대표 확인 결과 라이브 랜딩(`www.ipjuhae.com/`)의 역할별 화면이 여전히 "준비 중" 플레이스홀더로 노출되고 있었음. 원인: **목업 3종 커밋(`49a4d9b0`)은 9/4에 완성됐으나 push되지 않았고**, 그 사이 랜딩 전환 커밋(`86557460`)까지만 push·배포되어 구버전(플레이스홀더)이 라이브에 나감.

교훈: **커밋만 하고 push 안 된 상태는 라이브 기준 "안 된 것".** 워크로그에 반드시 push 여부·라이브 반영 여부를 명시할 것.

## 이번 세션에서 한 것

### 1. 디자인 시안 원본 확보 및 목업 대조

대표가 준 디자인 시안 — claude.ai/design "입주해 디자인" 프로젝트, 파일 **"입주해 MVP 화면.dc.html" (13화면)**:
- A · 발급 · 임차인 앱 4화면 (A1 안내 / A2 데이터 연결 / A3 공개 범위 / A4 발급 완료)
- B · 열람 · 임대인 2화면 + B3 중개사 검증 웹 1화면
- C · DataScore 3~4화면 (홈 / 항목 상세 / 변동 이력 / 산정 기준)

브라우저 경유로 13화면 전체 텍스트·비주얼 추출해 `waitlist-landing.tsx` 목업과 대조 → **랜딩 목업 3종은 시안의 A4/B1/B3와 텍스트·합성 데이터·팔레트(크림 #FBF6EF / 숯 #262220 / 오렌지 #F0663F / 브론즈 #B08D62) 일치** 확인. 코드 수정 불필요 판단.

### 2. 미push 커밋 3개 push → 프로덕션 배포

`49a4d9b0`(목업), `645a8dd7`(9/2 사업개발 문서), `d443c534`(9/4 워크로그) push.
- CI run 33935167434 ✅ 통과
- Fly Deploy run 33935318665 → 결과는 아래 배포 확인 참조

### 3. 배포 확인 (스모크)

- `/` 200 — 브라우저에서 역할별 탭 3종(임차인 카드 발급 / 임대인 열람 / 중개사 검증) 모두 실제 목업 렌더링 확인, "개발 중인 화면 · 합성 예시 데이터" 표기 확인
- `/home` 200, `/api/waitlist` 빈 POST 403 (same-origin CSRF 게이트의 정상 거부)
- `LAUNCH_SMOKE_BASE_URL=https://www.ipjuhae.com node scripts/launch-smoke.mjs` → 5개 중 4개 통과. 실패 1건은 `/api/launch/smoke`가 운영에서 `LAUNCH_SMOKE_TOKEN` 요구(403) — 토큰 없이 돌린 것이라 예상된 결과. 참고: 이 스크립트는 기본이 localhost:3000이므로 프로덕션 검증 시 반드시 `LAUNCH_SMOKE_BASE_URL` 지정.

## 추가 작업 (같은 날 2차): 서비스 미리보기 /preview

대표 지적: "서비스 미리보기(→/home)에 들어가면 메뉴가 안 보이고, 로그인 후 화면을 구경하려면 회원가입해야 한다."
- 원인: `Header`는 비로그인 시 `navLinks = []`로 메뉴를 숨기고, 메뉴 페이지들(/messages 등)은 API 401 시 /login으로 리다이렉트.
- 해결: **가입 없이 둘러보는 전용 화면 `/preview` 신설** (`components/preview/service-preview.tsx`). 로그인 후 메뉴 7종(프로필·Trust Card/인증/열람 기록/메시지/신뢰센터/거래/커뮤니티)을 실제 앱 헤더 구조 그대로 탭으로 전환하며 합성 예시 데이터로 구경. 상단에 미리보기 배너 + "사전 신청" CTA + "매물 검색 홈(/home)" 링크, 하단에 신청 CTA. noindex.
- 랜딩의 "서비스 미리보기" 진입점 2곳(헤더/푸터) `/home` → `/preview` 교체.
- 실제 페이지들을 게스트에 개방(빈 상태 노출/API 게스트 허용)하는 방안은 페이지·API 전반 수정이라 이번엔 보류 — 오픈 후 과제.
- 검증: typecheck·442테스트·build 통과, `/preview` 라우트 빌드 산출 확인, 배포 후 라이브 확인.

## 사전 대기열 현황 (2026-09-05 조회)

- 신청자 **1명**: tenant, 2026-09-04 신청 (대표 본인 테스트로 보임). invited/signed_up 없음.
- 조회 방법: `fly ssh console -a ipjuhae-production` 후 node+pg로 `ipjuhae.waitlist` 조회. 스키마 주의: 역할 컬럼은 `role`이 아니라 **`user_type`**.
- DB는 Neon Postgres(ap-southeast-1), 접속 문자열은 Fly secret `DATABASE_URL` (`fly ssh console -a ipjuhae-production -C "printenv DATABASE_URL"`로 확인, **저장소에 기록 금지**). DBeaver 등 GUI 접속 시 sslmode=require, 테이블은 `ipjuhae` 스키마.

## 추가 작업 (같은 날 3차): 기능 요구사항 소통창구

대표 제안: "신청자가 '이건 원한다'는 기능 요구사항을 남길 소통창구를 만들자."
- **migration-040**: `feature_requests` 테이블 (message ≤2000자, contact 선택, user_type 선택, source preview/landing). 런타임 DB에 plan→apply 완료 (applied 44→45).
- **API**: `POST /api/feature-requests` (공개, 미들웨어 same-origin 게이트 뒤) / `GET /api/admin/feature-requests` (관리자 전용, 최신순 200건).
- **UI**: `components/feedback/feature-request-form.tsx` — 랜딩 FAQ 아래 + `/preview` 하단에 삽입. 제출 성공 시 접수 안내로 전환.
- 라우트 테스트 7건 추가 (총 449 통과), typecheck·build 통과.
- 접수 확인 방법: DBeaver로 `ipjuhae.feature_requests` 조회, 또는 관리자 로그인 후 `GET /api/admin/feature-requests`.

## 추가 작업 (같은 날 4차): 랜딩 "특허 출원 중" 배지 제거

대표 지시로 랜딩 개인정보 안심 섹션의 "특허 출원 중" 배지 삭제. 사용자 노출 화면 전체를 grep해 이 한 곳뿐임을 확인(관리자·특허 내부 문서 제외). typecheck·449테스트·빌드 통과 후 push·배포.

소통창구 현황: 배포 이후 실제 접수 0건 (검증용 테스트 행은 삭제함).

## 추가 작업 (같은 날 5차): /preview 배너 "매물 검색 홈" 버튼 숨김

대표 지시로 미리보기 상단 배너의 `/home` 진입 버튼 제거 — 미리보기에서는 사전 신청 CTA만 남김. typecheck·449테스트 통과, 배포 후 라이브에서 문구 0건 확인. "특허 출원 중" 배지 제거도 같은 시점 라이브 반영 확인 완료.

## 추가 작업 (같은 날 6차): DataScore 섹션 재구성 — "보지 않는 것" 제거

대표 지시: "보지 않는 것은 표시하지 말자(나중에 어떻게 쓸지 모르니까). DataScore 개념과 조정 가능성만 알리고, 스코어링이 대단하다는 걸 어필해라."
- 랜딩 DataScore 섹션의 "보지 않는 것" 카드(나이·성별·국적·학력·직장·가족·신용등급 목록) 삭제, `NOT_COLLECTED` 상수 제거.
- 제목 "세입자의 신뢰를 100점으로 증명하는 DataScore" + 개념 소개문(임대 생활을 점수화한 입주해 고유 스코어링 모델) 추가.
- 자리에 "왜 DataScore인가" 카드: 블랙박스 없는 점수 / 서류 대신 점수 / 무이력 환산 / 매월 갱신. "조정될 수 있습니다" 문구 유지.
- 같은 기조 정리: FAQ "신용등급 조회?"를 "신용점수와 뭐가 다른가요?" 개념 답변으로 교체, 납부이력 설명·미리보기 신뢰센터의 "쓰지 않아요/수집하지 않습니다" 부정 약속 문구 제거. 사전신청 폼 개인정보 수집 안내(FAQ)는 법적 필요 문구라 유지.
- 449테스트·빌드 통과, 배포 후 라이브 확인("보지 않는" 0건, 새 카드 렌더링 확인).

## 추가 작업 (같은 날 7차): DataScore 양방향 개념 반영

대표 지적: "임차인·임대인 서로의 DataScore가 존재하는데 왜 세입자만 있냐." 확인 결과 트러스트 엔진(migration-024)은 `tenant/landlord/property` 3주체를 점수화하도록 설계돼 있으나 랜딩은 세입자 점수만 소개하고 있었음.
- 제목 "서로의 신뢰를 100점으로 증명하는 DataScore", 소개문에 양방향 원칙(세입자=임대 생활, 임대인·집=임대 이력·공적 기록) 반영.
- "왜 DataScore인가" 카드에 "양방향 점수" 항목 추가, 배점표에 "세입자 DataScore 배점" 라벨 명시(임대인·집 배점은 미확정이라 개념만), FAQ도 갱신.
- 449테스트·빌드 통과, 배포 후 라이브 문구 3종 확인 완료.

## 현재 상태 (세션 종료 시점)

- `main` = `origin/main` (이 로그 커밋 제외), 랜딩 목업 3종 **라이브 반영 완료**.

## 다음에 이어서 할 일 (우선순위순)

1. **아웃리치 발신자 정보 확정** — 대외 직함·회신 이메일·전화번호 대표 승인 필요 (서울광역청년센터 → 강동 → 관악 → 직방 순, 무첨부 절차 확인 메일).
2. **초기창업패키지 선정확인서 존재 확인** — 있으면 네이버클라우드 오픈이노베이션(9/11 마감) Go 전환.
3. SMS 프로바이더 미설정 — 전화번호만 남긴 waitlist 신청자에게 초대 발송 수단 없음.
4. (선택) 랜딩 목업 디테일 보강 — 시안 A4의 "전체 기록 보기 →"·"카드 정지하기", B3의 "최근 검증" 목록 등은 현재 랜딩용으로 생략된 상태. 대표 요청 시 추가.

## 작업 규칙 (대표 지시)

- 작업이 끝나면 반드시 커밋 + 이 형식의 워크로그. 미커밋으로 세션 종료 금지.
- **push·라이브 반영 여부까지 로그에 명시** (2026-09-05 추가).
