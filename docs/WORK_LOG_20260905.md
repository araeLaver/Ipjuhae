# 2026-09-05 작업 로그 — 랜딩 목업 3종 라이브 배포

## 문제 상황

대표 확인 결과 라이브 랜딩(`www.ipjuhae.com/`)의 역할별 화면이 여전히 "준비 중" 플레이스홀더로 노출되고 있었음. 원인: **목업 3종 커밋(`49a4d9b0`)은 9/4에 완성됐으나 push되지 않았고**, 그 사이 랜딩 전환 커밋(`86557460`)까지만 push·배포되어 구버전(플레이스홀더)이 라이브에 나감.

교훈: **커밋만 하고 push 안 된 상태는 라이브 기준 "안 된 것".** 워크로그에 반드시 push 여부·라이브 반영 여부를 명시할 것.

## 이번 세션에서 한 것

### 1. 디자인 시안 원본 확보 및 목업 대조

대표가 준 디자인 시안 — claude.ai/design "입주해 디자인" 프로젝트, 파일 **"입주해 MVP 화면.dc.html" (13화면)**:
- A · 발급 · 임차인 앱 4화면 (A1 안내 / A2 데이터 연결 / A3 공개 범위 / A4 발급 완료)
- B · 열람 · 임대인 2화면 + B3 중개사 검증 웹 1화면
- C · DataScore 3~4화면 (홈 / 항목 상세 / 변동 이력 / 산정 기준)

브라우저 경유로 13화면 전체 텍스트·비주얼 추출해 `waitlist-landing.tsx` 목업과 대조 → **랜딩 목업 3종은 시안의 A4/B1/B3와 텍스트·합성 데이터·팔레트(크림 #FBF6EF / 숯 #262220 / 오렌지 #F0663F / 브론즈 #B08D62) 일치** 확인. 코드 수정 불필요 판단.

### 2. 미push 커밋 3개 push → 프로덕션 배포

`49a4d9b0`(목업), `645a8dd7`(9/2 사업개발 문서), `d443c534`(9/4 워크로그) push.
- CI run 33935167434 ✅ 통과
- Fly Deploy run 33935318665 → 결과는 아래 배포 확인 참조

### 3. 배포 확인 (스모크)

- `/` 200 — 브라우저에서 역할별 탭 3종(임차인 카드 발급 / 임대인 열람 / 중개사 검증) 모두 실제 목업 렌더링 확인, "개발 중인 화면 · 합성 예시 데이터" 표기 확인
- `/home` 200, `/api/waitlist` 빈 POST 403 (same-origin CSRF 게이트의 정상 거부)
- `LAUNCH_SMOKE_BASE_URL=https://www.ipjuhae.com node scripts/launch-smoke.mjs` → 5개 중 4개 통과. 실패 1건은 `/api/launch/smoke`가 운영에서 `LAUNCH_SMOKE_TOKEN` 요구(403) — 토큰 없이 돌린 것이라 예상된 결과. 참고: 이 스크립트는 기본이 localhost:3000이므로 프로덕션 검증 시 반드시 `LAUNCH_SMOKE_BASE_URL` 지정.

## 현재 상태 (세션 종료 시점)

- `main` = `origin/main` (이 로그 커밋 제외), 랜딩 목업 3종 **라이브 반영 완료**.

## 다음에 이어서 할 일 (우선순위순)

1. **아웃리치 발신자 정보 확정** — 대외 직함·회신 이메일·전화번호 대표 승인 필요 (서울광역청년센터 → 강동 → 관악 → 직방 순, 무첨부 절차 확인 메일).
2. **초기창업패키지 선정확인서 존재 확인** — 있으면 네이버클라우드 오픈이노베이션(9/11 마감) Go 전환.
3. SMS 프로바이더 미설정 — 전화번호만 남긴 waitlist 신청자에게 초대 발송 수단 없음.
4. (선택) 랜딩 목업 디테일 보강 — 시안 A4의 "전체 기록 보기 →"·"카드 정지하기", B3의 "최근 검증" 목록 등은 현재 랜딩용으로 생략된 상태. 대표 요청 시 추가.

## 작업 규칙 (대표 지시)

- 작업이 끝나면 반드시 커밋 + 이 형식의 워크로그. 미커밋으로 세션 종료 금지.
- **push·라이브 반영 여부까지 로그에 명시** (2026-09-05 추가).
