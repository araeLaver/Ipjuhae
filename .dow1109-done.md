## 완료 — 사전 실패 6건 전부 해소, 전체 스위트 연속 녹색 확인

### 최종 검증

수정 후 전체 병렬 실행 2회 + 전 워크스페이스 타입체크 전부 통과했습니다.

| 실행 | 결과 | 소요 |
|---|---|---|
| 1회 (redaction 수정 전) | 172/172 파일, 888 통과 | 46.4s |
| 2회 (redaction 수정 전) | 172/172 파일, 888 통과 | 56.7s |
| 3회 (redaction 수정 전) | **1 실패** — 6번째 버그 발견 | 59.3s |
| 4회 (수정 후) | **173/173 파일, 890 통과 / 1 skip** | 61.7s |
| 5회 (수정 후) | **173/173 파일, 890 통과 / 1 skip** | 61.3s |

`pnpm -r typecheck` — 전 패키지 Done (exit 0).

파일·테스트 수가 172→173, 888→890으로 늘어난 것은 회귀 테스트를 추가했기 때문입니다.

### 해소한 실패 (티켓 원래 3항목 = 5건 + 추가 1건)

1. **`process-budget-guard.test.ts`** — 테스트가 낡은 것이었습니다(실제 회귀 아님). guard가 allowlist 필터를 우회하려고 호스트 env를 읽도록 **의도적으로** 바뀐 뒤 테스트가 따라오지 않았습니다. `vi.stubEnv` 기반으로 갱신하고, "자식 env로만 넘긴 노브는 무시된다"는 보안 동작 회귀 테스트를 추가했습니다.
2. **`IssueDocumentsSection.test.tsx` (2건)** — 고정 tick 대기(`flush()` 2회)가 원인. tick 횟수가 아니라 렌더 결과 조건을 기다리도록 교체했습니다.
3. **`opencode-local-adapter-environment.test.ts` (2건)** — 타임아웃 상향으로 덮지 않고 원인을 제거했습니다. `testEnvironment()`가 호출마다 개발자의 실제 `~/.config/opencode`를 통째로 재귀 복사하고 있었습니다(이 머신 6.5MB). 빈 config home을 가리키게 해서 복사를 무효화 — **5027/5002/4646ms → 33/162/246ms**. 부수적으로 호스트 설정에 의존하던 비-hermetic 문제도 없어졌습니다.
4. **(추가) `feedback-redaction.ts` UUID 손상** — 직전 코멘트 참고. 전화번호 정규식이 UUID 중간 세그먼트를 먹어 v4 UUID의 **6.85%를 훼손**하던 실제 프로덕션 버그였습니다. 경계를 `[\w-]`로 좁혀 **0.000%**로 해소.

### 티켓 범위를 넘어 같이 고친 것 — embedded Postgres 포트 충돌

직전 코멘트에서 "원인 미확정, 후속 티켓" 이라고 했던 `routines-e2e`의 FK 위반은 **원인을 찾아 함께 고쳤습니다.**

- 테스트용 embedded Postgres가 포트를 **OS ephemeral 범위(macOS 49152+)에서** 뽑고 있었습니다. Postgres는 `127.0.0.1`에, supertest 류는 wildcard에 bind하는데, **macOS는 loopback 전용 리스너가 이미 있어도 wildcard bind를 허용**합니다. 그 뒤 `127.0.0.1:<port>` 연결은 loopback 리스너(= 다른 워커의 Postgres)로 갑니다.
- 게다가 모든 테스트 인스턴스가 **동일한 계정·DB 이름**을 쓰기 때문에, 포트 경쟁에서 진 쪽이 남의 DB에 붙고도 **조용히 성공**합니다. 두 테스트 파일이 같은 DB를 공유하게 되고, 그 결과가 한참 뒤에 FK 위반이나 `Parse Error: Expected HTTP/`로 나타납니다.
- 조치 두 가지: (1) 포트를 ephemeral 범위 **아래**(20000~32000)에서 뽑아 겹침 자체를 제거, (2) 시작 후 `show data_directory`로 **그 포트의 서버가 내가 띄운 인스턴스가 맞는지 확인**하고 아니면 새 포트로 재시도. 조용한 성공 경로를 없앴습니다.

이전 코멘트에서 "데이터 디렉터리는 전부 각자 소유였으니 포트 충돌이 아니다"라고 썼던 건 틀렸습니다. 계측 시점에는 경쟁이 일어나지 않았을 뿐, 메커니즘 자체는 존재했습니다.

### 변경 범위와 커밋

저장소 `/Volumes/WorkDrive/Develop/45.paperclipai/paperclip`, 브랜치 `pending-restart`에 커밋 2건:

- `4115fe3a0` test: remove the load-dependent failures from the suite (8 파일)
- `8e4689caf` fix(feedback): stop the phone pattern from eating UUID segments (2 파일)

### 주의 — 배포에는 서버 재시작이 필요합니다

`feedback-redaction.ts`는 **프로덕션 코드**입니다. 이 저장소는 라이브 서버가 tsx로 직접 실행하는 소스지만 watch 모드가 아니라서, 재시작 전까지 런타임에 반영되지 않습니다. 즉 **지금 당장의 리스크도 없지만 효과도 없습니다.**

이미 재시작 대기 중인 `pending-restart` 브랜치에 쌓아 두었으므로, [DOW-1105](/DOW/issues/DOW-1105)의 SSH 소켓 수정이 재시작될 때 함께 반영됩니다. 별도 재시작을 요청하지는 않았습니다 — 재시작은 실행 중인 에이전트 런을 전부 죽이므로 판단이 필요한 사안입니다.

나머지 8개 파일은 테스트·테스트 설정이라 런타임 영향이 없습니다.
