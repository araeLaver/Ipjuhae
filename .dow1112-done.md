## 완료 — guard 이식 및 main docs/ 전수 점검 결과 보고

`npm run docs:public-check`가 main에서 동작합니다. 커밋 `e1e83c98`.

- `scripts/check-public-disclosure-terms.mjs` 이식 (원본 `87a6b9d9`, 206줄 → 247줄)
- `package.json`에 `docs:public-check` 추가

### 1. `DEFAULT_BASE_FILES` 처리 — 재지정 대신 "없으면 건너뛰되 경고"

기준 문서 2건은 [DOW-1111](/DOW/issues/DOW-1111)에서 복원될 예정이라, 경로를 다른 곳으로 재지정하면 복원 후 되돌리는 작업이 또 필요합니다. 그래서 **경로는 원본 그대로 두고, 없으면 제외하되 무엇이 빠졌는지 매 실행 경고로 출력**하게 했습니다.

```
경고: 기준 문서가 저장소에 없어 점검에서 제외됩니다 — docs/modoo-startup-feature-improvement-plan-20260723.md
경고: 기준 문서가 저장소에 없어 점검에서 제외됩니다 — docs/modoo-startup-patent-technical-implementation-plan-20260726.md
```

문서가 복원되면 코드 수정 없이 자동으로 다시 점검 대상이 됩니다. 조용히 건너뛰지 않은 이유는, 범위가 줄어든 검사가 "통과"로 보이는 게 실패보다 위험하기 때문입니다.

### 2. 이식 중 발견한 guard 자체의 결함 — `SQL`이 `PostgreSQL`을 잡습니다

원본은 `line.includes(term)`으로 단순 부분 일치를 합니다. 한글 용어는 조사가 붙으니 맞지만, **라틴 토큰에 같은 규칙을 쓰면 더 긴 단어 안쪽까지 잡힙니다.**

docs/ 전수 점검 **80건 중 26건(33%)이 `SQL` 하나의 오탐**이었습니다 — 전부 `PostgreSQL`, `MySQL`, 아키텍처 다이어그램의 `PG[(PostgreSQL)]` 같은 것들입니다. 결과의 3분의 1이 노이즈면 읽는 사람이 목록 전체를 무시하게 되므로 이식하면서 고쳤습니다. 라틴 문자로 시작하는 용어만 앞뒤 알파벳을 배제하고, 한글 용어 동작은 그대로입니다.

**80건 → 62건.**

### 3. main docs/ 전수 점검 결과 (62건 / 37개 파일)

| 용어 | 건수 | 성격 |
|---|---|---|
| 신용평가 | 23 | 대부분 **"이 표현을 쓰지 말라"는 정책 문구 자체** |
| 임계값 | 10 | 내부 기획 문서의 "산식·임계값을 작성해야 한다" 류 |
| JWT_SECRET | 9 | 워크로그의 **환경변수명·에러 메시지 인용** |
| SQL | 8 | 워크로그의 "조회 SQL 미첨부" 등 실제 언급 |
| DATABASE_URL | 5 | 체크리스트의 **환경변수명** |
| 특허 등록 | 2 | 변리사 상담 녹취 1건 + **"등록 가능성을 판단하지 않는다"는 면책 문구** 1건 |
| 자동 계약조건 확정 / 원본 서류 공개 | 2 | **guard가 제시하는 대체 문구 표 자체** |

**실제 비밀값 유출은 0건입니다.** 전부 환경변수 이름이거나 에러 메시지 인용입니다.

과장 표현 4건도 전수 확인했고 **전부 오탐**입니다:
- `상담녹음_260629.txt:89` — 변리사가 "특허 등록받을 수 있게 도와드린다"고 말한 녹취. 내부 기록이지 대외 주장이 아닙니다.
- `기율법무법인_260720_통합현황분석.md:121` — `특허 등록 가능성이나 ... 판단하는 것이 아니다`. 면책 문구인데 허용 패턴이 `/아닌/`·`/아니라/`만 갖고 있어 **"아니다"를 못 걸러냅니다.**
- `기능반영_전체매트릭스_260723.md:317,319` — 금지어와 대체 문구를 나란히 적은 **대조표 자체**.

### 4. CI 연결 — 붙이지 않는 것을 권고합니다 (결정 요청: @CTO)

- **docs/ 전수를 CI에 거는 것은 반대합니다.** 62건이 전부 기존 문서이고 실질 위반은 0건입니다. 지금 걸면 CI가 빨간 채로 시작하고, 어느 팀이나 그 다음 수순은 무시하거나 꺼버리는 것입니다.
- **기본 대상(최근 2일 이내 날짜 문서 + 기준 문서)은 현재 5건**이며, 이것도 전부 워크로그의 환경변수명 인용이라 그대로 걸면 실패합니다.
- 제 권고는 **대외 제출 직전 수동 게이트로 운영**하는 것입니다. 제출 문서 경로를 인자로 직접 넘기면 됩니다: `node scripts/check-public-disclosure-terms.mjs docs/제출문서.md`
- CI 자동화를 원하신다면 선행 작업이 필요합니다: (a) `ALLOWED_CONTEXT_PATTERNS`에 `아니다`·`피한다`·`오해`·`환경변수명` 추가, (b) 내부 워크로그(`docs/WORK_LOG_*`, `docs/01.기율법무법인/**`) 제외 규칙. 필요하다고 판단하시면 후속 티켓으로 올리겠습니다.

### 참고

`git add`를 제 파일만 지정했는데도 다른 에이전트가 같은 워킹트리 인덱스에 올려둔 파일 3건(`__tests__/api/account-delete-atomicity.test.ts` 외)이 첫 커밋에 딸려 들어갔습니다. 되돌려 제 파일 2건만 커밋했고, 그 3건은 **내용 해시 동일한 채로 원래 staged 상태로 복원**해 두었습니다.
