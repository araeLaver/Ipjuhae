## 수정 커밋 완료 — 다음 크래시가 곧 이 수정을 배포합니다

원인 분석은 본문에 적었고, 고쳤습니다. 커밋 `171a066a5` (`pending-restart`, `packages/db/src/backup-lib.ts` 단일 파일).

### 고친 것 2가지 — 둘 다 있어야 합니다

**1. 행 덤프를 커서 스트리밍으로**

```ts
// before — 테이블 하나를 통째로 JS 배열로 물질화
const rows = await sql.unsafe(`SELECT * FROM ${table}`).values();

// after — 500행씩 흘려보냄
const rowCursor = sql.unsafe(`SELECT * FROM ${table}`).values().cursor(500);
for await (const batch of rowCursor) { ... }
```

힙 사용량이 **테이블 크기가 아니라 배치 크기의 함수**가 됩니다.

**2. writer에 `drain()`을 넣고 배치마다 await**

커서만으로는 부족했습니다. `emit()`은 동기 함수라 `pendingWrite` 체인에 `.then()`을 얹기만 합니다. 덤프 루프가 동기로 계속 돌면 **이벤트 루프에 제어가 안 넘어가서 청크가 하나도 실제로 쓰이지 않고 체인에 쌓입니다.** 배치마다 `await writer.drain()`을 넣어 이미 큐에 들어간 쓰기가 끝난 뒤 다음 배치를 당겨옵니다.

이 2번이 빠지면 커서를 넣어도 메모리가 안 잡힙니다.

### 기존 테스트가 왜 못 잡았나

`backup-lib.test.ts`에 이미 `"...without materializing one giant string"`이라는 테스트가 있습니다. 이전에 누군가 **writer 쪽(거대 문자열)** 은 고쳤는데, **행 쪽(`SELECT *`)** 이 남아 있었습니다. 지금 테스트는 통과하지만 행 물질화를 검증하지 않습니다 — 회귀 테스트를 이어서 추가합니다.

### 검증 (현재까지)

- `pnpm --filter @paperclipai/db exec tsc --noEmit` — 통과
- `packages/db/src/backup-lib.test.ts` — 2 passed

### 배포 경로 — 재시작 요청이 필요 없습니다

`server`의 dev 스크립트는 `tsx src/index.ts`로 watch가 없어서, 실행 중인 프로세스는 **옛 코드를 메모리에 들고 있습니다.** 그런데 이 서버는 어차피 ~62분마다 죽고 launchd(`KeepAlive=true`)가 되살립니다.

즉 **다음 크래시가 곧 이 수정의 배포입니다.** 계획 외 재시작을 따로 요청할 필요가 없습니다 — 현재 프로세스(pid 46123, 08:19:59 기동)는 이 글을 쓰는 시점 기준 몇 분 안에 죽을 차례입니다. 워킹트리가 `pending-restart`에 있고 커밋이 거기 얹혀 있어 그대로 실려 갑니다.

이 heartbeat가 그 크래시로 중간에 끊길 수 있습니다. 끊기면 다음 웨이크에서 이어받아 검증하겠습니다.

### 남은 일

1. **회귀 테스트 추가** — 큰 테이블을 덤프할 때 힙이 평평하게 유지되는지 고정
2. **검증** — 백업 주기 3회(약 3시간)를 크래시 없이 넘기는지, 새 크래시 리포트가 안 생기는지
3. **보존(retention) 미동작** — 파일 505개 / 731GB, 가장 오래된 것이 48일 전인데 `retentionDays=30`. 별도 후속으로 분리 예정
4. **launchd 로그 0바이트** — 포렌식 공백. 위와 함께 후속
