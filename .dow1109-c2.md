## 진행 상황 — 이전 런이 검증 도중 유실되어 재개, 6번째 사전 실패를 새로 찾았습니다

이전 런은 `process_lost_retry`로 검증 도중 끊겼습니다. 작업 트리(`/Volumes/WorkDrive/Develop/45.paperclipai/paperclip`, 브랜치 `pending-restart`)에 변경은 그대로 남아 있었고, 거기서 이어서 검증했습니다.

### 전체 스위트 재검증 (3회)

| 실행 | 결과 | 소요 |
|---|---|---|
| 1회 | 172/172 파일, 888 통과 / 1 skip | 46.4s |
| 2회 | 172/172 파일, 888 통과 / 1 skip | 56.7s |
| 3회 | **1 실패** / 887 통과 | 59.3s |

원래 보고한 5건은 3회 모두 통과했습니다. 3회차에서 **전혀 다른 테스트**가 실패했습니다.

### 새로 찾은 6번째 실패 — 테스트 flake가 아니라 실제 데이터 손상 버그

`server/src/__tests__/feedback-service.test.ts`

```
expected '0a37527d-[REDACTED_PHONE]-9cee-f1a7e9…'
      to be '0a37527d-3236-4782-9cee-f1a7e99d7715'
```

원인은 `server/src/services/feedback-redaction.ts:67`의 전화번호 마스킹 정규식입니다. 경계가 `\w`만 배제하므로 **UUID 중간의 숫자 전용 세그먼트가 전화번호로 인식됩니다.** `0a37527d-3236-4782-...`에서 `3236-4782`는 앞뒤가 하이픈이라 lookaround를 그대로 통과합니다.

- 테스트가 `randomUUID()`를 쓰기 때문에 **생성된 UUID에 따라 걸리기도 하고 안 걸리기도 하는** 실행 의존 실패였습니다. 부하와 무관해서 1·2회차에는 나타나지 않았습니다.
- 랜덤 v4 UUID 20만 개로 측정: **6.85%가 손상**됩니다.
- 영향은 테스트에 그치지 않습니다. 이 함수는 공유 피드백 번들의 본문·로그·어댑터 트레이스 전체를 거칩니다(`feedback.ts`에서 14곳 호출). 즉 **번들을 추적 가능하게 만드는 run id·issue id 자체가 15개 중 1개꼴로 훼손**된 채 나갑니다. 번들을 만드는 이유를 스스로 무너뜨리는 버그입니다.

### 조치

경계를 `\w`에서 `[\w-]`로 좁혀, 하이픈으로 이어진 더 긴 토큰의 **일부만** 전화번호로 잡히는 일을 없앴습니다. 전화번호 자체는 하이픈이 매치 **안쪽**에 있으므로 그대로 마스킹됩니다.

- 측정: UUID 손상률 **6.85% → 0.000%**
- 전화번호 3종(`555 111 2222`, `010-1234-5678`, `+1 (415) 555-2671`) 전부 여전히 전체 마스킹되는 것 확인
- 회귀 테스트 `server/src/__tests__/feedback-redaction.test.ts` 신규 추가 (UUID 보존 / 전화번호 마스킹 양방향)

**이번 변경은 프로덕션 코드입니다** — 앞선 코멘트에서 "테스트만 건드렸다"고 했던 범위를 벗어납니다. 정규식 경계 한 곳이고, 서버는 재시작 전까지 워킹트리 변경이 런타임에 반영되지 않으므로 지금 당장의 런타임 리스크는 없습니다. 다만 **실제로 배포되려면 서버 재시작이 필요**합니다.

### 남은 작업

- 수정 후 전체 스위트 2회 추가 실행 + `pnpm -r typecheck` 진행 중
- 결과가 확인되면 커밋하고 이 티켓에 마무리 보고하겠습니다.
